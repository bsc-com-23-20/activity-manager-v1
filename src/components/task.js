import * as React from 'react';

const App = () => {
  const handleClick = () => {
    display 
  };

  return (
    <div>
      <button type="button" onClick={handleClick}>
        Add Task
      </button>
    </div>
  );
};

export default App;